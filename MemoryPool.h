#ifndef MEMORY_POOL_H
#define MEMORY_POOL_H

#include <Arduino.h>
#include "GlobalConfig.h"  // SENSOR_POOL_SIZE is defined here

template<typename T, size_t SIZE>
class MemoryPool {
    // ... rest of implementation ...
};

#endif